# Writing Scenarios

Scenarios are declarative experiment definitions inside `uwatt.yaml`.

Checkpoint 0 validates scenario structure and rejects unknown properties. Later checkpoints
will execute scenarios through synthetic and physical measurement backends.

