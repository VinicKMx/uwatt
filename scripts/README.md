# Scripts

Repository maintenance and developer automation scripts live here.

Checkpoint 0 uses the `Makefile` for checks. Future scripts should be deterministic,
documented and avoid controlling hardware without explicit configuration.

